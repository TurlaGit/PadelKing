# PadelKing Bot — Техническое задание (SPEC)

Единый источник правды по продукту. Все решения зафиксированы здесь.
Язык интерфейса — только русский. Хранилище — SQLite. Google Sheets не используется.

---

## 1. Роли

- **Игрок** — участник турнира. Взаимодействует с ботом в личке + видит анонс в группе.
- **Админ (Яна и др.)** — управляет турнирами и оплатами через админку **внутри бота**.
  Список админов — `ADMIN_IDS` в `.env` (несколько Telegram user_id).
- **Разработчик** — поддерживает код через Claude Code + GitHub + сервер.

---

## 2. Ключевые принципы / ограничения Telegram

- Бот редактирует (`editMessageText`) только **своё** сообщение → анонс публикует бот, не человек.
- Бот **не может написать первым** тому, кто не запускал бота (или заблокировал). Обработка — см. §5.3.
- Идентификатор человека — числовой **user_id** (постоянный). Ник (@username) — только для отображения, может меняться/отсутствовать.

---

## 3. Сущности (модель данных)

### tournaments
- `id` (slug/uuid), `title`, `format` (тип: Americana/…), `date`, `time_start`, `time_end`
- `location_id` → locations
- `game_format` (свободный текст, многострочный: корты/раунды/минуты/сет)
- `levels` (список выбранных уровней)
- `price`, `currency` (IDR/USD/RUB) — **за человека**
- `max_pairs` (NULL = без лимита)
- `extra_note` (необязательный доп. текст)
- `start_at` (полный datetime со временем, пояс **Asia/Makassar / WITA UTC+8**)
- `payment_deadline` = `start_at − 24ч` (вычисляется)
- `publish_at` (NULL = опубликован/сразу; иначе отложенная публикация)
- `announce_chat_id`, `announce_message_id`, `pinned` (bool)
- `status`: draft / scheduled / published / finished / cancelled

### locations (сохранённый справочник)
- `id`, `title`, `maps_url`

### levels_custom, formats_custom (пользовательские добавления к кнопкам)
- `id`, `value`

### registrations (запись = ПАРА)
- `id`, `tournament_id`
- `player_user_id`, `player_name`, `player_username`
- `partner_user_id` (NULL пока не подтверждён/не привязан), `partner_name`, `partner_username`
- `is_looking_for_partner` (bool) — слот «🔍 ищет партнёра»
- `status`: looking / pending_confirm / active / waitlist / cancelled / removed_unpaid
- `slot_index` (порядок в составе), `created_at`

### payments (статус оплаты по КАЖДОМУ игроку)
- `id`, `registration_id`, `who` (player/partner), `user_id`
- `status`: unpaid / pending_review / paid / paid_manual / rejected
- `screenshot_file_id` (Telegram file_id), `amount_hint` (NULL, без OCR в v1)
- `reviewed_by`, `reviewed_at`
- `reminder_stage` (какое напоминание уже отправлено), `last_reminder_at`

### pair_requests (заявки «хочу в пару»)
- `id`, `tournament_id`, `from_user_id`, `to_registration_id`
- `status`: pending / accepted / declined / expired / cancelled
- `created_at`, `expires_at`

---

## 4. Игрок: главное меню

В любой момент `/start` или «🏠 В меню»:
- 🏆 Список турниров (активные/будущие) → карточка → «✅ Записаться»
- 📋 Мои записи (статусы, оплата)
- ❓ Связь с админом (@zhurakovskaya)

**«⬅️ Назад» / «🏠 В меню» — на каждом экране.**

---

## 5. Игрок: запись на турнир

### 5.1 Старт записи
По кнопке «Записаться» (в анонсе через deep-link `?start=<tid>` или из меню) бот в личке:
1. Показывает инфо о турнире.
2. Спрашивает имя игрока (по умолчанию подставляет имя из Telegram, можно изменить).
3. Спрашивает: «С кем играешь?»
   - **[👥 Знаю партнёра]** → ввести @ник партнёра.
   - **[🔍 Ищу партнёра]** → слот «ищет партнёра».

### 5.2 Лимит и лист ожидания
- Если основной состав (`max_pairs`) заполнен → пара идёт в **лист ожидания** (`status=waitlist`).
- В анонсе показываются основной состав и лист ожидания отдельными блоками.

### 5.3 Партнёр указан по @нику
- Бот ищет партнёра Б в БД по нику.
  - **Б известен боту** (когда-либо запускал) → бот пишет Б **напрямую**: «Тебя записали в пару с А на турнир X. Подтверди участие [✅ Подтвердить] [❌ Отказаться]». Игрок А **не** получает ссылку для пересылки.
  - **Б неизвестен/заблокировал** → А получает **готовое сообщение для пересылки** Б («Привет! Тебя записали в пару на турнир X. Подтверди — нажми старт: <ссылка>»). Когда Б открывает бота по ссылке и жмёт старт — это считается согласием, пара привязывается.
- Исключение (правило сыгранных пар): если А подал обоих **и сразу оплатил за двоих** → Б добавляется **автоматически, без подтверждения**.

### 5.4 «Ищет партнёра» — заявки (НЕ мгновенный захват)
В анонсе слот: `N. @userA + 🔍 ищет партнёра  [✋ Хочу в пару]`
- Б жмёт «Хочу в пару» → Б: «Запрос отправлен @userA, ждём ответа». А: «🤝 @userB хочет с тобой в пару. [✅ Принять] [❌ Отклонить]».
- **Принять** → пара формируется, обоим — реквизиты на оплату; остальные заявки к А автоматически закрываются («@userA выбрал другого партнёра»).
- **Отклонить** → Б получает выбор:
  - [👥 Записаться со своим партнёром]
  - [🔍 Записаться отдельно и искать партнёра]
  - [🤝 Подать заявку другому одиночке] (список ищущих)
  - [⏳ Встать в лист ожидания]
  - [🏠 В меню]
- **Таймаут заявки:** 24 часа, но не позже дедлайна оплаты (что раньше) → авто-`expired`, Б получает те же варианты.
- А сам нашёл партнёра вне бота / удалился → все висящие заявки к нему → «@userA уже не ищет партнёра».

### 5.5 Отмена самим игроком
Кнопка «❌ Отменить запись»:
- Слот освобождается, анонс обновляется.
- Если был основной состав и есть лист ожидания → первая пара из листа поднимается (см. §7.3).
- Если у А был партнёр Б (пара сформирована) → Б становится одиночкой и получает каскадное сообщение (§5.6).

### 5.6 Каскад: партнёр выбыл
Сообщение затронутому игроку Б:
> ⚠️ Твой партнёр @userA отменил участие в турнире «X». Вы больше не в паре. Что дальше?

Кнопки:
- [👥 Указать нового партнёра]
- [🔍 Остаться и искать партнёра] (стать одиночкой в списке)
- [🤝 Подать заявку тому, кто ищет пару] (список одиночек)
- [❌ Выписаться из турнира]
- [🏠 В меню]

---

## 6. Оплата

### 6.1 Реквизиты
Сразу после формирования пары обоим игрокам — реквизиты (Bank OCBC + TRC20) с подстановкой
{title}/{date}/{weekday}/{price}. Дедлайн = `start_at − 24ч`.

### 6.2 Кто платит
У каждого игрока выбор: **[Оплатить за себя] / [Оплатить за обоих]** (можно сменить позже).

### 6.3 Скрин оплаты
- Игрок присылает фото/документ в бот → статус `pending_review`, ответ: «Спасибо, скрин получен, передали на проверку ✅».
- **OCR не используется (v1)** → расходов на API нет. Сумму проверяет Яна глазами.
- Профилактика: в тексте про оплату — «📸 Скрин присылай только сюда, в бот. В личку организатору — не засчитается».

### 6.4 Проверка Яной (в админке)
Карточка проверки (приходит пушем + доступна в очереди «Оплаты на проверке»):
```
🧾 Скрин от @userA  (пара: @userA + @userB)
Игрок указал: «за себя» — ожидается 350.000 IDR (за двоих 700.000)
[✅ Принять за себя] [👥 Принять за двоих] [❌ Отклонить]
```
- **За себя** → этот игрок `paid`, второй продолжает висеть.
- **За двоих** → оба `paid`, обоим «Оплата принята».
- **Отклонить** → игроку: «Оплата не принята. Пришли корректный скрин сюда или свяжись с @zhurakovskaya».
- При `paid` → останавливается цепочка напоминаний игроку, ему: «✅ Оплата принята, ждём на турнире! 🎾».

### 6.5 Ручное подтверждение (без скрина)
Для кейса «прислал скрин Яне в личку напрямую» или нал/форс-мажор. В админке у пары:
- [✅ Подтвердить обоих]
- [✅ Подтвердить @userA]
- [✅ Подтвердить @userB]
→ статус `paid_manual` (метка просто «оплачено»), уведомления игроку не дублируются лишний раз.

### 6.6 Добавить пару вручную
Из админки «➕ Добавить пару» — Яна вписывает ники, помечает «оплачено». Таким **напоминания не шлются**.

---

## 7. Дедлайны и напоминания (планировщик)

Бот раз в минуту сканирует БД и шлёт то, что пора. Restart-safe (всё в БД).
Время считается в поясе **WITA (UTC+8)**.

### 7.1 Стандартная цепочка (от дедлайна = start−24ч)
- При записи → реквизиты + дедлайн.
- За 24ч до дедлайна (если не оплачено) → мягкое напоминание.
- **За 1 час до дедлайна** → жёсткое игроку + **Яне уведомление**: «у пары @… остался час до снятия» + **полный список неоплативших**.
- В дедлайн → авто-снятие неоплаченных пар, слот освобождается, дублируется Яне.

Текст жёсткого (за час):
> ⏰ Остался 1 час на оплату турнира «X». Если оплата не поступит до HH:MM, бронь снимется и место освободится. Если уже оплатил — пришли скрин сюда.

### 7.2 Авто-снятие
Дедлайн прошёл, оплаты нет → `removed_unpaid`, анонс обновляется, подтягивается лист ожидания.

### 7.3 Поднятие из листа ожидания
Поднятой паре (дедлайн основного состава уже мог пройти) — **окно 5 часов** (настраивается):
```
T+0     «Вас подняли из листа ожидания. Оплатите в течение 5 часов.»
T+2:30  «осталось ~2:30»
T+4:00  жёсткое игроку + уведомление Яне (за час до снятия)
T+5:00  авто-снятие → дублируется Яне
```

---

## 8. Анонс в группе

- Постит и редактирует **бот**. Кнопки под анонсом: «✅ Записаться» (deep-link), «🔗 Открыть в Maps» (если есть ссылка), «✋ Хочу в пару» у слотов-одиночек.
- Авто-дописывается «‼️ Участие подтверждается после оплаты».
- Состав обновляется в реальном времени при каждом изменении.

### Пины
- Турнир закреплён, пока он **будущий**. Несколько будущих → несколько закреплённых (Telegram складывает в «N закреплённых»).
- Бот **открепляет** анонс, когда турнир прошёл (`start_at + ~2ч`). Точное время известно из `start_at`.

---

## 9. Админка (в боте, только для ADMIN_IDS)

### 9.1 Визард «Создать турнир»
1. Название
2. Формат (тип) — кнопки Americana/Mexicano/King of the Court/Mixed/Stars + «Свой…»
3. Дата (ДД.ММ)
4. Время начала–конца
5. Локация — сохранённые + «➕ Добавить новую» (название+ссылка → сохранить в справочник)
6. Формат игры — **свободный текст**
7. Уровни — **мультивыбор** галочками (Beginner / Low·Mid·High Bronze / Silver / Gold) + «Свой…» + «✅ Готово»
8. Стоимость — валюта (IDR/USD/RUB) + число (за человека)
9. Макс. пар — число или «Без лимита»
10. Ещё что-то? — «Пропустить» / текст
11. **Превью** → [✏️ Изменить шаг] [🔄 Заполнить заново] [📤 Опубликовать]
12. Публикация → [📤 Сейчас] / [🕐 Запланировать на дату-время] (отложенный постинг)

Дедлайн, кнопки анонса, строка про оплату — добавляются автоматически.

### 9.2 Уровни (справочник)
Полный набор: Beginner, Low Bronze, Mid Bronze, High Bronze, Low Silver, Mid Silver, High Silver,
Low Gold, Mid Gold, High Gold. «Свой…» сохраняется и появляется в кнопках в след. раз.

### 9.3 Прочие разделы админки
- **Оплаты на проверке** (очередь скринов).
- **Оплаты по турниру** `/оплаты <турнир>` — таблица статусов.
- **Список неоплативших** (по турниру).
- **➕ Добавить пару вручную** / ручное подтверждение оплаты.
- **Локации** — управление справочником.
- **Аналитика:**
  - Топ-10 самых частых участников (с числом записей).
  - **Итоги турнира** — собранная сумма (💰) в любой момент.

---

## 10. Хранение и приватность

- Скрины оплат содержат банковские данные → **автоудаление через 14 дней после турнира** (запись «оплачено/сумма» остаётся, файл удаляется). Настраивается.
- Имена/ники/история записей — хранятся (для аналитики/рейтинга), не чувствительны.
- Доступ к скринам — только админы.

---

## 11. Хостинг и передача

- Бот работает 24/7 на сервере (Render/Railway, ~$5–7/мес). Polling.
- SQLite на постоянном диске.
- Передача: **(1)** разворачивается на сервере, доступы передаются клубу; **(2)** код — в отдельном репозитории клуба; **(3)** Claude Code-плагин (скиллы + доки) для будущего разработчика; **(4)** Яна управляет всем через админку — ставить ничего не нужно.

---

## 12. Открытые/настраиваемые значения (дефолты)

- Таймаут заявки в пару: **24ч** или до дедлайна.
- Окно оплаты для поднятых из листа ожидания: **5ч**.
- Удержание скринов: **14 дней** после турнира.
- Пояс времени: **Asia/Makassar (WITA, UTC+8)**.
