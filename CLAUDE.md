# CLAUDE.md — гайд по проекту PadelKing Bot

Telegram-бот для записи на падел-турниры клуба (Бали). Подробное ТЗ — в **SPEC.md**.

## Стек
- Python 3.12, **aiogram 3.x**, polling
- **SQLite** (aiosqlite) на постоянном диске — единственное хранилище (Google Sheets убран)
- Планировщик: собственный цикл «скан раз в минуту» (restart-safe), без внешних либ
- Тексты/справочники — частично в `content.md`, турниры создаются через админку и живут в БД
- Деплой — Render/Railway (worker), `render.yaml` готов

## Запуск локально
```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env   # вписать BOT_TOKEN, GROUP_CHAT_ID, ADMIN_IDS
python main.py
```

## Переменные окружения (.env)
- `BOT_TOKEN` — токен @BotFather
- `BOT_USERNAME` — `PadelFirst_bot`
- `GROUP_CHAT_ID` — группа клуба (-100…)
- `ADMIN_CHAT_ID` — главный админ для уведомлений
- `ADMIN_IDS` — кому доступна админка (через запятую)
- `DB_PATH` — путь к SQLite (на Render: `/var/data/padel.db`)

## Ветка разработки
`claude/telegram-padel-bot-aiogram-YSM2Q`. Коммитим осмысленно, пушим туда же.
Финальная передача клубу — отдельный репозиторий (создаётся в конце).

## Архитектура модулей (целевая)
```
main.py            — старт, polling, запуск планировщика
config.py          — env
db.py              — слой SQLite (tournaments, registrations, payments, pair_requests, locations…)
content.py         — статические тексты (приветствие, реквизиты-шаблон)
content.md         — реквизиты, контакты, дефолтные тексты
formatting.py      — рендер карточек/анонса/реквизитов, даты, уровни
keyboards.py       — все inline-клавиатуры (+ «Назад»/«В меню» везде)
states.py          — FSM-состояния (запись игрока, визард админки)
handlers/
  player.py        — меню, запись, партнёр, заявки, отмена, оплата (приём скрина)
  admin.py         — визард турнира, очередь оплат, ручное подтверждение, аналитика
announcement.py    — пост/редактирование анонса, пины
scheduler.py       — напоминания, авто-снятие, отложенная публикация, чистка скринов
```

---

## ПЛАН РАБОТ (по шагам)

Статус: `[ ]` не начато · `[~]` в работе · `[x]` готово

### Этап 0 — подготовка ✅
- [x] Скелет бота (анонс, deep-link, БД, реквизиты, уведомление админу)
- [x] Реальные реквизиты + динамический шаблон оплаты ({weekday}/{date}/…)
- [x] Убрать Google Sheets
- [x] SPEC.md + CLAUDE.md
- [ ] Деплой текущей версии на сервер (стенд для тестов) — ждёт аккаунт хостинга

### Этап 1 — ядро «пары» ✅
- [x] Новая схема БД (registrations как пара, payments по игроку, pair_requests, locations)
- [x] FSM записи: имя → партнёр (знаю/ищу)
- [x] Партнёр по нику: прямой DM или сообщение-пересылка (§5.3)
- [x] Слот «ищет партнёра» + заявки [✋ Хочу в пару] → принять/отклонить (§5.4)
- [x] Лимит пар + лист ожидания, отмена + каскад партнёра (§5.5–5.6)
- [x] Анонс: блоки «состав / лист ожидания / ищут партнёра», кнопки

### Этап 2 — оплаты ✅
- [x] Реквизиты обоим, выбор «за себя/за обоих» (+ смена)
- [x] Приём скрина → `pending_review`
- [x] Очередь проверки в админке + ✅за себя/👥за двоих/❌ (§6.4)
- [x] Ручное подтверждение по игроку, добавить пару вручную (§6.5–6.6)

### Этап 3 — планировщик ✅
- [x] Цикл «скан раз в минуту», пояс WITA
- [x] Цепочка напоминаний + уведомление Яне за час + список неоплативших (§7.1)
- [x] Авто-снятие + поднятие из листа ожидания (окно 5ч) (§7.2–7.3)
- [x] Отложенная публикация анонсов
- [x] Автоудаление скринов через 14 дней
- [x] Пины: закрепить будущие, открепить прошедшие (§8)
- [x] Авто-протухание заявок в пару (24ч)

### Этап 4 — админка-визард ✅
- [x] Визард «Создать турнир» (11 шагов + превью + изменить шаг/заново) (§9.1)
- [x] Справочники: локации, уровни-мультивыбор, форматы, «свой…»
- [x] Публикация: сейчас / запланировать
- [x] Аналитика: топ-10 участников, итоги турнира (💰)

### Этап 5 — передача
- [ ] Отдельный репозиторий клуба
- [ ] Claude Code-плагин (скиллы + доки) для будущего разработчика
- [ ] Инструкция Яне по админке

---

## Договорённости (кратко, детали в SPEC.md)
- Идентификатор — user_id; отображаем @ник.
- OCR в v1 не используем (нет расходов на API); сумму проверяет Яна.
- Бот не пишет первым тем, кто не запускал его → пересылка только в первый раз.
- Несколько админов (`ADMIN_IDS`), несколько турниров параллельно.
- Везде кнопки «⬅️ Назад» и «🏠 В меню».
